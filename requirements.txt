# Dependencias para BUILD de JupyterLite (se ejecutan en GitHub Actions)
jupyterlite-core>=0.2.0
jupyterlab>=4.0.0
jupyterlite-pyodide-kernel>=0.2.0

# NOTA: Los paquetes Python como numpy, matplotlib, pandas
# se instalan automáticamente en el navegador mediante Pyodide
# No necesitan estar aquí

